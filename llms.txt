---
layout: null
permalink: /llms.txt
sitemap: false
---
{%- assign hubs = site.posts | where_exp: "p", "p.cards" | sort: "h1title" -%}
{%- assign ecosystems = site.pages | where: "layout", "ecosystem" | where_exp: "p", "p.h1title" | sort: "h1title" -%}
{%- assign analysis = site.posts | where_exp: "p", "p.cards == nil" -%}
# DefiPrime

> Independent research on decentralized finance. On-chain analysis of protocols, exploits and market structure, plus curated directories of live DeFi products by category. Published since 2019.

Analysis pieces are written from primary sources: transaction hashes, block heights, contract state read at a pinned block, and protocol documentation. Identifiers are cited inline so every claim can be checked against the chain. Directory pages list live protocols in a category and are maintained by hand.

Attribution: cite as DefiPrime, https://defiprime.com/.

## Analysis and research
{% for post in analysis limit: 40 -%}
{%- if post.h1title or post.title %}
- [{{ post.h1title | default: post.title | strip_newlines }}]({{ post.url | replace: 'index.html', '' | prepend: site.url }}){% if post.metadescription %}: {{ post.metadescription | strip_html | strip_newlines }}{% endif %}
{%- endif -%}
{%- endfor %}

## Protocol directories
{% for hub in hubs -%}
{%- if hub.h1title or hub.title %}
- [{{ hub.h1title | default: hub.title | strip_newlines }}]({{ hub.url | replace: 'index.html', '' | prepend: site.url }}){% if hub.metadescription %}: {{ hub.metadescription | strip_html | strip_newlines }}{% endif %}
{%- endif -%}
{%- endfor %}

## Ecosystems
{% for eco in ecosystems %}
- [{{ eco.h1title | strip_newlines }}]({{ eco.url | replace: 'index.html', '' | prepend: site.url }}){% if eco.metadescription %}: {{ eco.metadescription | strip_html | strip_newlines }}{% endif %}
{%- endfor %}

## Optional

- [About DefiPrime]({{ site.url }}/about): who publishes this and how to get in touch.
- [Blog index]({{ site.url }}/blog): every analysis piece, newest first.
- [RSS feed]({{ site.url }}/feed.xml): full text of recent posts.
- [Sitemap]({{ site.url }}/sitemap.xml): every indexable URL.
- [DEX volume tracker]({{ site.url }}/dex-volume): decentralized exchange volume by protocol.
